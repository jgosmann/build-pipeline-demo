format_version: 10
pipelines:
  build:
    group: defaultGroup
    materials:
      repo:
        type: configrepo
    stages:
      - build:
          clean_workspace: true
          tasks:
            - exec:
                command: ./gradlew
                arguments:
                  - build

